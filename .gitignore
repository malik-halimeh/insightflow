# Dependencies
node_modules

# Nuxt build artefacts
.nuxt
.output
.data
dist

# Local env — never commit real credentials
.env
.env.*
!.env.example

# Logs and OS noise
*.log
.DS_Store

# Private planning notes — kept local, never pushed
PLANNING-PHASE-SUMMARY.md
